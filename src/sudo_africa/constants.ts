export const Sudo_AFRICA_SANDBOX_URL = 'https://api.sandbox.sudo.cards';
export const SUDO_AFRICA_LIVE_URL = 'https://api.sudo.africa';

export enum ENVIRONMENT {
  PRODUCTION = 'production',
  SANDBOX = 'sandbox',
}
